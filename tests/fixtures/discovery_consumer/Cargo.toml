[package]
name = "discovery-consumer-fixture"
version = "0.1.0"
edition = "2024"

[dependencies]
discovery-provider-fixture = { path = "../discovery_provider" }
qubit-event-bus = { path = "../../..", features = ["discovery"] }
qubit-spi = "0.13"
